{
  "name": "donovan-family-dentistry-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots",
    "check:launch": "node scripts/check-launch-readiness.mjs",
    "predev": "node scripts/check-launch-readiness.mjs && node scripts/materialize-assets.mjs && node scripts/patch-patient-form.mjs",
    "prebuild": "node scripts/check-launch-readiness.mjs && node scripts/materialize-assets.mjs && node scripts/patch-patient-form.mjs"
  },
  "dependencies": {
    "astro": "7.1.4",
    "pdf-lib": "1.17.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.0",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
